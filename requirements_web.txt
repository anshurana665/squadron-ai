# OpenSquad AI — Web Version Dependencies

# Backend API
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# Frontend server
flask>=3.0.0

# OpenSquad core (already installed)
# langgraph
# openai
# fpdf2

# Already present in your environment:
# streamlit  (still used for Streamlit mode)
